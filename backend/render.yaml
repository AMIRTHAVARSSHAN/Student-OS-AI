services:
  - type: web
    name: scholar-os-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        value: postgresql+asyncpg://postgres.iaykhpsrmptokiantgcc:amirtha2009%40@aws-0-ap-south-1.pooler.supabase.com:6543/postgres
